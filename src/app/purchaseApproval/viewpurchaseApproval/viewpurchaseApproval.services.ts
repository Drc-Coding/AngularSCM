import {Injectable} from '@angular/core';
import {Http, Response, Headers, RequestOptions} from '@angular/http';
@Injectable()
export class viewpurchaseApprovalService {
  private sesUrl = '/api/';
  private sDetailscUrl = '/api/';
  constructor(private http: Http) {}

  
}
